import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette:{
        primary:{
            main:'red',
            light:'#808080'
        },
        secondary: {
            main: '#edf2ff',
        },
    }
})
